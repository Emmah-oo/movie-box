import React, { useEffect } from "react";
import { useParams } from "react-router-dom";

const MovieDetails = () => {
//   const { id } = useParams();
  useEffect(() => {

  }, []);

  return (
    <div>
      <h1>hry</h1>
    </div>
  );
};

export default MovieDetails;
